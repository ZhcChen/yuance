app_key: "yuance"
name: "元策正式环境"
environment: "production"
app_type: "compose"
deploy_strategy: "rolling_stop_on_failure"
health_check_kind: "http"
health_endpoint: "http://127.0.0.1:33033/api/healthz"
health_timeout_secs: 5
health_expected_status: 200

notes:
  - "正式环境运行在 qfy-test2:/srv/yuance/backend；qfy-test2 是当前内网优先发布目标。"
  - "目标机运行目录只执行 docker load 和 docker compose up；禁止 cargo build、docker build 或源码打包。"
  - "发布前先备份 SQLite，再通过单次维护容器执行 migrate status、migrate up、seed core。"
  - "首次超级管理员由用户访问 /web 手动初始化，不执行 seed demo 或 seed local-admin。"
  - "OSS 在 /web/system/storage 动态配置，部署 .env 不写 OSS AccessKey。"
