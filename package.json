{
  "name": "yuance",
  "private": true,
  "scripts": {
    "check:frontend": "npm --prefix web run check && npm --prefix frontend run check && npm --prefix desktop run check:renderer",
    "validation:prepare": "./scripts/local-validation.sh prepare",
    "validation:api": "./scripts/local-validation.sh api",
    "validation:web": "./scripts/local-validation.sh web",
    "validation:desktop": "./scripts/local-validation.sh desktop",
    "validation:status": "./scripts/local-validation.sh status"
  }
}
